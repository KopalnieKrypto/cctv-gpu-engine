"""client-agent placeholder package (issue #16).

The real Flask UI + RTSP recorder + R2 uploader land in #7 / #8. This
module exists today only so the Docker image (issue #16 acceptance) has a
runnable ENTRYPOINT and `docker compose up` keeps the container alive.
"""
